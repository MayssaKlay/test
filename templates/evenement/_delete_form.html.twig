<form method="post" action="{{ path('evenement_delete', {'id_event': evenement.id_event}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.id_event) }}">
    <button style="margin-left: 900px;" class="btn btn-danger btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-trash"></i>
                                        </span>
        <span class="text">Supprimer l'évenement</span>
    </button>
</form>


